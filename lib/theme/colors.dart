import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  static const green = Colors.green;
  // static const red = Colors.redAccent;
  static const red = Color.fromARGB(255, 164, 80, 203);
}
